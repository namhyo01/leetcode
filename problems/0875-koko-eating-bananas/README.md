# 875. Koko Eating Bananas

- 난이도: **Medium**
- 유형: Binary Search (답을 이분탐색 / Parametric Search)
- 링크: https://leetcode.com/problems/koko-eating-bananas/

## 문제

코코는 바나나를 좋아한다. `n`개의 바나나 더미가 있고, `piles[i]`는 `i`번째 더미의 바나나 개수다.
경비원이 `h`시간 뒤에 돌아온다.

코코는 시간당 먹는 속도 `k`(정수)를 정한다. **매 시간마다 더미 하나를 골라 `k`개를 먹는다.**
더미에 `k`개보다 적게 남아 있으면 남은 걸 다 먹고, **그 시간에는 더 먹지 않는다.**
(= 더미 하나를 여러 시간에 걸쳐 먹을 수는 있어도, 한 시간에 두 더미를 건드릴 수는 없다)

경비원이 돌아오기 전에 모든 바나나를 다 먹을 수 있는 **최소 정수 속도 `k`** 를 반환하라.

## 제약 조건

- `1 <= piles.length <= 10^4`
- `piles.length <= h <= 10^9`
- `1 <= piles[i] <= 10^9`

## 예제

```
입력: piles = [3,6,7,11], h = 8
출력: 4

입력: piles = [30,11,23,4,20], h = 5
출력: 30

입력: piles = [30,11,23,4,20], h = 6
출력: 23
```

## 스스로 체크할 것

- [ ] `k`를 1부터 하나씩 올려보면 정답은 나온다. 근데 `piles[i] <= 10^9`인데 그게 통과할까?
- [ ] 속도 `k`가 주어졌을 때 **몇 시간 걸리는지** 계산하는 식은? (`ceil` 주의)
- [ ] `k`가 커지면 걸리는 시간은 어떻게 되나? 이 성질에 이름을 붙인다면?
- [ ] 탐색 범위의 **하한과 상한**은 각각 무엇으로 잡아야 하나?
- [ ] 시간 합이 `int` 범위를 넘길 수 있나? (`k=1`, `n=10^4`, `piles[i]=10^9`이면?)

## 내 접근 (풀면서 채우기)

<!--
- 처음 떠올린 방법:
- 막힌 지점:
- 최종 아이디어:
- 시간복잡도:      / 공간복잡도:
-->
